# E2E 测试环境变量配置示例
# 复制此文件为 .env.test 并填写实际值
# cp .env.test.example .env.test

# === Google Cloud 项目配置 ===
GCP_PROJECT_ID=your-project-id

# === Service Account 配置 ===
# E2E 测试使用的 Service Account Email
JWT_TEST_SERVICE_ACCOUNT=your-service-account@your-project-id.iam.gserviceaccount.com

# === JWT 测试配置 ===
# 测试用的 Audience（可以是任意 URL）
JWT_TEST_AUDIENCE=https://your-test-service.run.app/

# === 数据库配置（E2E 测试用） ===
# 如果需要真实数据库测试，取消注释并填写
# DATABASE_URL=postgresql://user:pass@host:5432/dbname?sslmode=require

# === 其他测试配置 ===
E2E_TEST_TIMEOUT=30
E2E_TEST_VERBOSE=true
