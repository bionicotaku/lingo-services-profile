# Example environment variables for Profile Service

# PostgreSQL connection string (Supabase or local Postgres)
DATABASE_URL=postgresql://postgres:[YOUR_PASSWORD]@localhost:54321/profile?sslmode=require

# 可选：覆盖 gRPC 监听端口（默认为 config.yaml 中的 0.0.0.0:9000）
PORT=9000

# 可选：运行环境标签（development / staging / production），影响日志与指标
APP_ENV=development

# 可选：服务名称与版本（写入可观测性维度）
SERVICE_NAME=feed
SERVICE_VERSION=dev

# 可选：自定义配置文件路径（默认使用 configs/config.yaml）
# CONF_PATH=/absolute/path/to/custom-config.yaml
